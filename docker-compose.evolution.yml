version: '3.8'

services:
  evolution_db:
    image: postgres:15-alpine
    container_name: evolution_db
    restart: always
    environment:
      POSTGRES_DB: evolution
      POSTGRES_USER: coecaribe_user
      POSTGRES_PASSWORD: DBPasswordSecure2026
    volumes:
      - evolution_db_data:/var/lib/postgresql/data
    # No exponemos el puerto 5432 al VPS para evitar cualquier conflicto con tu PostgreSQL existente.
    # Solo será accesible para el contenedor de la API dentro de la red interna de Docker.

  evolution_api:
    image: evoapicloud/evolution-api:latest
    container_name: evolution_api
    restart: always
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - SERVER_URL=http://167.88.45.94:8080
      - API_KEY=ClinicaCoecaribe2026
      # Configuración de Base de Datos
      - DATABASE_PROVIDER=postgresql
      - DATABASE_CONNECTION_URI=postgresql://coecaribe_user:DBPasswordSecure2026@evolution_db:5432/evolution?schema=public
      # Deshabilitar telemetría y cosas innecesarias
      - DEL_INSTANCE_TEMP_FOLDER=true
    depends_on:
      - evolution_db

volumes:
  evolution_db_data:
